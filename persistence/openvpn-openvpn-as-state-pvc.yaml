apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    volume.beta.kubernetes.io/storage-provisioner: rancher.io/local-path
    volume.kubernetes.io/selected-node: moo
  labels:
    app.kubernetes.io/instance: openvpn
    app.kubernetes.io/name: openvpn-as
  name: openvpn-openvpn-as-state
  namespace: openvpn
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: local-path
  volumeName: pvc-a29afaac-3a43-4a0c-bbe0-f9f907e4c7d4
