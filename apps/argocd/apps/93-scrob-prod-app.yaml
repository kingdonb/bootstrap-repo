### This Application doesn't function correctly because the destination
### namespace isn't matching, so downstream resources aren't visualized.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: 93-scrob-prod
  namespace: argocd
spec:
  destination:
    namespace: scrob-production
    server: https://kubernetes.default.svc
  project: default
  source:
    path: ./
    targetRevision: HEAD
  syncPolicy:
    syncOptions:
    - ApplyOutOfSyncOnly=true
    - FluxSubsystem=true
