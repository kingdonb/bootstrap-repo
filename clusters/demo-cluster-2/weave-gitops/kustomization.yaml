apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: podinfo
resources:
  - weave-gitops.yaml
configMapGenerator:
  - name: weave-gitops-values
    files:
      - values.yaml=values.enc.yml
configurations:
  - kustomizeconfig.yaml
