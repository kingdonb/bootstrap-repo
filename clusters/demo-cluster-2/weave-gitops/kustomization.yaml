apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: podinfo
resources:
  - weave-gitops.yaml
secretGenerator:
  - name: weave-gitops-values
    files:
      - values.yaml=values.yml.encrypted
configurations:
  - kustomizeconfig.yaml
